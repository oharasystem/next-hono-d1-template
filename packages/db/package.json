{
  "name": "@next-hono-d1-template/db",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply next-hono-d1-template-db --local --config ../../apps/api/wrangler.toml",
    "db:migrate:remote": "wrangler d1 migrations apply next-hono-d1-template-db --remote --config ../../apps/api/wrangler.toml",
    "studio": "drizzle-kit studio",
    "lint": "eslint src/"
  },
  "dependencies": {
    "drizzle-orm": "^0.33.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.24.0",
    "typescript": "^5.5.0",
    "tsx": "^4.19.2",
    "@cloudflare/workers-types": "^4.20240529.0"
  }
}
